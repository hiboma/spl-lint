index="idx_alpha_events" category=credentials_failed
| eval @email='target_user.email'
| eval @ip='client.ip_address'
| eval @hostname='client.platform_name'
| eventstats count by @email
| where count >= 30
| stats values(*) as * by @email
